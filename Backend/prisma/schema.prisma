generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Esquema mínimo de fundación; los dominios (users, teams, tournaments…) se añadirán en vertical slices.
